Sportify_Server/
│── Sportify_Server/         # Thư mục chính của project
│   │── __init__.py    
│   │── settings.py    # Cấu hình toàn bộ project Django
│   │── urls.py        # Khai báo các đường dẫn (routes)
│   │── asgi.py        # Cấu hình ASGI server (chạy async Django)
│   │── wsgi.py        # Cấu hình WSGI server (chạy Django trên Gunicorn, Apache, ...)
│── manage.py          # Dùng để chạy server, tạo migrations, quản lý project
│── myapp/             # Mỗi app là một module độc lập
│   │── migrations/    # Chứa các file migration của database
│   │── admin.py       # Đăng ký model vào Django Admin
│   │── apps.py        # Cấu hình ứng dụng (app) trong Django
│   │── models.py      # Khai báo model (bảng) trong database
│   │── serializers.py # Chuyển đổi dữ liệu giữa model và JSON
│   │── views.py       # Xử lý logic của request (API, trang HTML)
│   │── urls.py        # Định nghĩa các URL riêng của app
